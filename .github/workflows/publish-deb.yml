name: Publish .deb to PPA

on:
  push:
    branches:
      - main
    tags:
      - "*"

jobs:
  lint-test:
    if: github.repository == 'barebaric/rayforge'
    uses: ./.github/workflows/lint-test.yml

  publish-to-ppa:
    name: Build and publish to PPA
    needs: [lint-test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y devscripts debhelper dput build-essential python3-all dh-python

      - name: Import GPG key
        run: |
          echo "${{ secrets.PPA_GPG_PASSPHRASE }}" | gpg --batch --yes --passphrase-fd 0 --import <(echo "${{ secrets.PPA_GPG_PRIVATE_KEY }}" | base64 --decode)
          echo "--- GPG Public Keys ---"
          gpg --list-keys
          echo "--- GPG Secret Keys ---"
          gpg --list-secret-keys

      - name: Install Pixi
        uses: prefix-dev/setup-pixi@v0.8.1
        with:
          pixi-version: latest

      - name: Build source package
        env:
          DEB_GPG_PASSPHRASE: ${{ secrets.PPA_GPG_PASSPHRASE }}
        run: pixi run build-deb-source

      - name: Upload to PPA
        env:
          DEB_GPG_PASSPHRASE: ${{ secrets.PPA_GPG_PASSPHRASE }}
        run: |
          dput ppa:knipknap/rayforge dist/*.changes
